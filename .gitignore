# Brainstorm companion — mockups and session state
.superpowers/

# Never commit recordings or derived artefacts
/archive/
/media/
*.wav
*.WAV

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/

# hatch-vcs generated version file (don't track, don't change)
src/fledermap/__about__.py

# Tooling caches
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Local secrets
*.pwd
.env

# Dev-only DB dumps from scripts/db-backup.sh (scripts/db-restore.sh restores)
/.db-backups/
.worktrees/
